@model IEnumerable<MasProjekt.Lokalizacja>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Powierzchnia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Powierzchnia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Lokalizacja_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Lokalizacja_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Lokalizacja_ID })
        </td>
    </tr>
}

</table>

<center>
    <br />
    <button class="btn btn-success" onclick="location.href='@Url.Action("Index", "Szablon_uroczystości")';return false;">Przeglądaj szablony</button>
    <button class="btn btn-success" onclick="location.href='@Url.Action("Create", "Weseles")';return false;">Utwórz wesele!</button>
</center>
